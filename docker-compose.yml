services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - backend/.env.production
    ports:
      - "8000:8000"
    volumes:
      - backend-projects:/data/projects
    restart: unless-stopped

volumes:
  backend-projects:
